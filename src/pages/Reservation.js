import { Nav, BookingForm } from "../components";
import { Container } from "@mui/material";

const Reservation = () => {
  return (
    <Container>
      <Nav />
      <BookingForm />
    </Container>
  );
};

export default Reservation;
